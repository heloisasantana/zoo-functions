const data = require('../data/zoo_data');

//Começando o projeto! Uhu!

function countEntrants(entrants) {
  // seu código aqui
}
a
function calculateEntry(entrants) {
  // seu código aqui
}

module.exports = { calculateEntry, countEntrants };
